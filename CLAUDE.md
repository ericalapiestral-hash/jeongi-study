# CLAUDE.md — 전기산업기사 필기 스터디 앱

> 이 파일은 포맷으로 날아간 대화 기록을 대신해, 저장소 현재 상태(README·git 로그·소스 구조)에서 복원한 프로젝트 맥락입니다. 사실과 다른 부분이 있으면 바로 고쳐 주세요.

## 이 프로젝트가 뭔가

전기 지식이 **하나도 없는 상태에서 시작해**, 게임처럼 배우면서 문제를 푸는 전기산업기사 필기 학습 앱. 설치 없이 브라우저에서 돌아가고, 오프라인에서도 공부할 수 있다 (PWA).

배포: https://ericalapiestral-hash.github.io/jeongi-study/

## 스택 — 중요

**순수 HTML/CSS/JavaScript. 빌드 도구·프레임워크·외부 라이브러리 없음.**

`index.html`을 더블클릭하면 그냥 돈다. 이 제약이 이 프로젝트의 설계 전제다 — npm 패키지나 번들러를 도입하려면 먼저 상의할 것.

## 구조

```
index.html      화면 골격 · 스크립트 로딩
style.css       전체 스타일 (모바일 반응형)
app.js          핵심 로직 — 문제 풀이 · SRS · 통계 · XP · 힌트
journey.js      여정 모드 (커리큘럼 · 노드 진행)
data.js         문제은행 (345KB)
lessons.js      이론 레슨 (337KB)
formulas.js     공식 도감 (53KB)
dojo-data.js    계산 도장 문제 (63KB)
dojo.js         계산 도장
more.js         부가 기능 (버그 헌트 · 주간 성적표 · 미니게임 허브)
fun.js          일일 퀘스트 · 저금통 · 문지기 · 스트릭 방패
boss.js         보스 레이드
codex.js        공식 도감
lab.js          전기 실험실
crunch.js       벼락치기 코스
ghost.js        어제의 나 고스트 (과거 기록과 속도 경쟁)
garden.js       기억 정원 (SRS를 화분으로 시각화)
rogue.js        전력망 탐험 (로그라이크 카드 모드)
sw.js           오프라인 캐시 (캐시 우선)
manifest.json   앱 설치 정보 · 아이콘들
serve.py        로컬 서버      폰으로 공부하기.bat
twa/            Android APK (TWA) — GitHub Actions로 자동 빌드
.github/workflows/build-apk.yml
```

**로딩 순서 주의** — `ghost.js`·`garden.js`·`rogue.js`는 `app.js` 뒤, `journey.js` 앞에 와야 한다.
`journey.js`가 로드 시점에 `renderHome()`을 호출하기 때문이다.
`rogue.js`는 `boss.js`의 `SFX`·`actx`도 쓰므로 `boss.js`보다 뒤여야 한다.

`data.js`·`lessons.js`가 각각 340KB 안팎이라 **전체를 컨텍스트에 올리지 말 것.** 필요한 항목만 검색해서 볼 것.

## 콘텐츠 규모

여정 모드 28스테이지·129단계 / 이론 레슨 129개 / 문제 258개(본문제 + 쌍둥이 확인문제) / 공식 31개 / 미니게임 6종 / 보스 5마리 / 실험 8종 / 탐험 카드 10종·유물 8종

## 학습 설계 (건드릴 때 주의)

- **확신도 베팅** — 답을 고른 뒤 `확실 / 반반 / 찍음` 선택. **찍어서 맞힌 문제는 마스터로 인정하지 않고 다시 출제**한다 — 예상 점수가 부풀지 않게 하는 장치다.
- **착각 구역** — "확실"인데 틀린 문제를 따로 모아 최우선 복습.
- **간격 반복(SRS)** — 맞힌 개념도 1·3·7·14·30일 간격으로 재출제.
- **정서 안전장치** — 연속으로 틀리면 이미 마스터한 쉬운 문제를 끼워넣어 세션을 정답으로 마무리한다.
- 그 외: 일일 퀘스트, 60점 저금통, 주간 성적표, 오답노트(틀린 이유 태그 → 약점 레이더 → 맞춤 처방)

이 네 가지는 단순 기능이 아니라 **의도된 학습 설계**다. 성능이나 단순화를 이유로 걷어내지 말 것.

### 힌트 (2026-08-17 추가)

막혔을 때 문제를 포기하지 않게 하는 장치. 2단계로 나뉜다.

1. **선택지 2개 지우기** — 오답만 골라 지운다. XP ×0.6, SRS는 정상 진행 (스스로 고른 것이므로).
2. **개념 먼저 보기** — XP ×0.35, **SRS 레벨을 올리지 않고 내일 다시 물어본다.**

2단계를 "찍음"과 같이 취급하는 게 핵심이다. 개념을 열어보고 맞힌 걸 마스터로 세면
확신도 베팅으로 막아둔 **예상 점수 부풀리기가 힌트로 되살아난다.** 이 규칙을 풀지 말 것.
복습 문제는 개념이 이미 화면에 있어 1단계까지만, 모의고사는 힌트가 아예 없다.

### 어제의 나 고스트 (2026-08-17 추가)

같은 코스(`모드:과목:문제수`)를 다시 풀면 과거 최고 기록이 반투명 러너로 함께 달린다.
- **문제를 붙잡고 있던 시간만** 잰다 — 해설 읽는 시간·확신도 고르는 시간은 양쪽 다 뺀다.
- 한 문제 2분 상한 (자리 비움 방지), 기록 갱신은 **정답 수 우선, 같으면 빠른 쪽**.
- 상대가 과거의 자신이라 **지든 이기든 자존감이 상하지 않는 것**이 설계 의도다.
  가짜 라이벌·순위표로 바꾸지 말 것 (조사에서 명시적으로 걸러진 항목).

### 기억 정원 (2026-08-17 추가)

"복습할 개념 N개"라는 숫자를 화분으로 바꾼 것. SRS 레벨이 화분 단계(🌱→🌳),
복습일이 지날수록 시든다(🥀). 탭하면 마른 화분만 모아 푸는 `물 주기` 세션.
**새 저장 구조 없이 `S.srs`만 읽어서 그린다.**

### 전력망 탐험 — 로그라이크 카드 모드 (2026-08-17 추가)

`rogue.js`. 8개 층을 올라가며 문제로 싸우고, 이기면 카드를 얻어 덱을 키운다.
층마다 갈래를 고르고(전투·강적·모닥불·보물·상점·보스), 체력 ❤️ 6으로 시작해 틀리면 깎인다.

**세 가지 설계 원칙 — 이걸 어기면 이 모드는 공부를 방해하는 물건이 된다.**

1. **카드는 자원이 아니라 "문제 푸는 도움"이다.** 소거(선택지 2개 지우기)·복기(개념 보기)·
   집중(피해 증가)·절연(오답 무효) 같은 것들이다. 뽑기·수집·꾸미기 축으로 바꾸지 말 것 —
   조사에서 `부품 뽑기`·`내 배전반 꾸미기`가 "수집에 쓰는 시간이 문제 푸는 시간을 대체한다"는
   이유로 걸러졌다. 여기서 무언가를 모은다면 그건 **문제를 더 잘 풀기 위한 것**이어야 한다.
2. **런에서 푼 문제도 전부 `recordAttempt`로 SRS·통계에 정상 반영된다.**
   게임 시간이 곧 공부 시간이어야 한다. 이 호출을 빼면 놀이와 공부가 분리되어
   앱을 켠 시간이 점수로 이어지지 않는다.
3. **실패해도 잃는 것이 없다.** XP·복습 기록·오답노트 전부 그대로 남는다.
   문구도 "패배"가 아니라 **"이번 탐험은 여기까지"**로 쓴다. 두 번 낙방한 사람에게
   손실 페널티는 재미가 아니라 스트레스다 (`승부수 사다리`가 걸러진 이유와 같다).
   **손실을 거는 도박 구조를 넣지 말 것.**

런 상태는 `S.rogue`에 통째로 저장되고 매 행동마다 `saveState()`한다 —
폰에서 앱을 닫아도 전투 중간부터 이어진다. 런이 끝나면 `S.rogue = null`,
누적 기록만 `S.rogueStats`(runs·clears·bestFloor)에 남는다.

## 문제 정확성 방침

- 모든 문제는 출제 후 **별도 검증으로 정답을 교차 확인**했고, 계산 문제는 수식으로 재계산해 검산했다.
- 전기산업기사 필기는 **2021년 CBT 전환 이후 기출이 공개되지 않는다.** 이 앱 문제는 기출을 옮긴 게 아니라 빈출 유형을 분석해 같은 수준으로 새로 만든 오리지널이다.
- 전기설비기술기준은 **KEC(2021~) 기준**을 따른다.

문제를 추가·수정할 때 이 기준을 유지할 것 — 검산 없이 정답을 넣지 않는다.

## 기록 보관

진도·오답노트·XP는 **서버로 전송되지 않고 기기 브라우저에만** 저장된다. 기기마다 따로 쌓인다. 저장소 영구 모드(persistent storage)를 요청하도록 해뒀다.

## 배포 · 설치

- **웹**: GitHub Pages
- **폰**: 위 주소 → 크롬 메뉴(⋮) → 홈 화면에 추가 (오프라인 동작). 설치 배너를 직접 호출하는 코드가 있다.
- **APK**: `twa/` + GitHub Actions로 자동 빌드. 파일명은 **영문으로 유지** — 한글명은 깨진다.
- **집 안 네트워크**: `폰으로 공부하기.bat` 실행 후 표시되는 주소로 접속

## 알아둘 것

- 아이폰 오프라인을 위해 PNG 아이콘을 넣고 서비스워커를 **캐시 우선**으로 전환해둔 상태다.
  `sw.js`를 고치면 `CACHE` 버전, `index.html`의 `style.css?v=`, `FILES` 목록을 **한 세트로** 올려야 갱신이 먹는다.
  캐시 우선이라 파일을 고친 직후 첫 로드는 **구버전이 뜨는 게 정상**이다 (한 번 더 새로고침해야 반영).
- 홈 최상단에 **"바로 시작" 버튼 + 간단 모드**가 있다 — 앱이 커지면서 진입 부담을 줄이려고 넣은 것.
- **홈은 의도적으로 접어놨다** (2026-08-17). 항상 보이는 건 인사·급한 알림 1개·주요 버튼 3개·정원·퀘스트·저금통뿐이고,
  나머지 모드 6개와 도구·과목 목록은 `➕ 다른 방법으로 공부하기`로 접혀 있다.
  이 학생의 최대 실패 지점이 **"앱을 열고도 뭘 할지 몰라 닫는 것"**이라, 카드를 다시 펼쳐놓지 말 것.
  급한 알림은 착각 구역 > 오답 게이지 순으로 **하나만** 띄운다.
- **벼락치기 코스**(`crunch.js`)는 시험 임박 시 D-day 맞춤 전략을 짜준다.
- `재미 아이디어 조사결과.md` — 게임화 아이디어 조사 메모. 새 기능 논의 전에 참고.

## 최근 작업 흐름 (git 로그 기준, 최신순)

로그라이크 카드 모드(전력망 탐험) → 홈 접기 + 힌트 + 고스트 + 기억 정원 → 홈 "바로 시작" 버튼 + 간단 모드 → APK 다운로드 링크 안내 → APK 파일명 영문화 → APK 자동 빌드(TWA + Actions) → 벼락치기 코스 → 앱 설치 배너 → 저장소 영구 모드 + 아이폰 설치 안내 → 아이폰 오프라인 지원

## 코드 스타일

한국어 커밋 메시지, 한국어 UI/주석. 파일명은 영어. 모듈 시스템 없이 `index.html`에서 스크립트를 순서대로 로드하므로 **전역 이름 충돌에 주의**할 것.
